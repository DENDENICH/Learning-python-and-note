# Дескриптором же называют объект со связанным поведением,
# то есть такой объект, чьё поведение при доступе к нему как к атрибуту переопределяется методами протокола дескрипторов:
# __get__(), __set__() и __delete__().
# Если хотя бы один из этих методов определён для объекта, то он считается дескриптором


